function Global(x, y, rotation, scaleX, scaleY, intensity, skeyX, skewY) {
    this.x = x
    this.y = y
    this.rotation = rotation
    this.scaleX = scaleX
    this.scaleY = scaleY
    this.intensity = intensity
    this.skeyX = skeyX
    this.skewY = skewY
}
